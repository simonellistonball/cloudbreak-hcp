kerberos:
  key: RANDOM
  password: RANDOM
ambari:
  adminPassword: RANDOM  
credentialName: sball-se
hcpVersion: 1.7
# Cloud provider values can be (aws, ycloud, openstack)
cloudProvider: aws
# yarn queue if the cloud provider is 'ycloud' else neglect
yarnQueue: HCS-integration-tests

nifi:
  security:
    encrypt:
      configuration:
        password: RANDOM
  sensitive:
    props:
      key: RANDOM
  toolkit:
    tls:
      token: RANDOM
  registry:
    security:
      encrypt:
        configuration:
          password: RANDOM
    sensitive:
      props:
        key: RANDOM

ranger:
  kms:
    password: RANDOM
